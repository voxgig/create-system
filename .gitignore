node_modules
dist
dist-test
*.log
*.tsbuildinfo
.DS_Store

# Coverage output (CI uploads it as an artifact)
coverage.lcov
*.lcov

# Vale downloads the Google package here on `vale sync`; the vocabularies
# and house rules beside it are committed.
.vale/styles/Google/
